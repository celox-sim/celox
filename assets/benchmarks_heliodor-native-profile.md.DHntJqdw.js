import{_ as o,o as a,c as i,ah as t}from"./chunks/framework.ojwJKwrc.js";const m=JSON.parse('{"title":"Heliodor Native Backend Profile Notes","description":"","frontmatter":{},"headers":[],"relativePath":"benchmarks/heliodor-native-profile.md","filePath":"benchmarks/heliodor-native-profile.md"}'),l={name:"benchmarks/heliodor-native-profile.md"};function c(s,e,d,r,n,u){return a(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="heliodor-native-backend-profile-notes" tabindex="-1">Heliodor Native Backend Profile Notes <a class="header-anchor" href="#heliodor-native-backend-profile-notes" aria-label="Permalink to &quot;Heliodor Native Backend Profile Notes&quot;">​</a></h1><p>This note records the current Heliodor <code>test_soc_linux_boot</code> profile for the native backend and the optimization directions that were measured and rejected.</p><h2 id="baseline" tabindex="-1">Baseline <a class="header-anchor" href="#baseline" aria-label="Permalink to &quot;Baseline&quot;">​</a></h2><p>Reference run:</p><ul><li>Veryl cc boot: <code>65.667s</code></li><li>Veryl boot cycles: <code>0x009ae070 = 10,150,000</code></li><li>Veryl cc <code>perf stat</code>: <code>636,928,081,861</code> host instructions, IPC <code>2.02</code></li></ul><p>Celox native sample:</p><ul><li>120 second timeout reached about <code>1,389,676</code> ticks</li><li><code>avg_apply_us=2.547</code></li><li><code>avg_comb_us=59.893</code></li><li>Celox <code>perf stat</code> over 80 seconds: <code>736,078,011,950</code> host instructions, IPC <code>1.83</code></li></ul><p>The dominant gap is host instructions per simulated tick, not host IPC. Veryl cc is about <code>62.8k</code> host instructions per tick for this benchmark. Celox native is roughly an order of magnitude higher.</p><h2 id="tenfold-gap-model" tabindex="-1">Tenfold Gap Model <a class="header-anchor" href="#tenfold-gap-model" aria-label="Permalink to &quot;Tenfold Gap Model&quot;">​</a></h2><p>The roughly tenfold gap is too large to explain with register allocation alone. The current data decomposes the problem as follows:</p><ul><li>Veryl cc boots <code>10,150,000</code> simulated cycles in <code>65.667s</code>, about <code>6.47 us</code> per simulated cycle.</li><li>Celox native spends about <code>59.893 us</code> per tick in <code>eval_comb</code> alone, before counting <code>eval_apply_ff</code>.</li><li>Celox <code>eval_comb</code> has about <code>263k</code> MIR instructions after normal MIR optimization, before regalloc.</li><li>Regalloc then expands this to about <code>357k</code> MIR instructions.</li></ul><p>So the allocator expansion is a serious multiplier, but removing all regalloc overhead would not make the code competitive by itself. The native backend is already generating several times too much scalar work before allocation.</p><p>The likely structural causes are:</p><ul><li>bitfield and trigger/dirty updates are lowered as many independent scalar load/modify/store sequences;</li><li>constants, masks, and boolean results are represented as SSA VRegs too often instead of being kept as immediates, flags, or memory operands at x86 lowering time;</li><li>very large merged <code>eval_comb</code> blocks create high live pressure and poor locality;</li><li>regalloc then amplifies this shape by repeatedly spilling long-lived boolean/bitfield transients and reloading rematerialized constants.</li></ul><p>This means the fix needs two layers: reduce the pre-regalloc scalar work, then make allocation pressure-aware. Only doing one side is unlikely to recover the full order of magnitude.</p><p>The hot <code>eval_comb.bb0</code> SIR block confirms that the pre-regalloc work is already large. A dump of block 0 after SIR optimization contained <code>72,345</code> SIR instructions. The largest instruction classes were:</p><ul><li><code>And</code>: <code>13,068</code></li><li><code>Mux</code>: <code>11,808</code></li><li><code>LogicAnd</code>: <code>8,234</code></li><li><code>Eq</code>: <code>6,944</code></li><li><code>Shr</code>: <code>6,350</code></li><li><code>Load</code>: <code>5,107</code></li><li><code>Concat</code>: <code>4,607</code></li><li><code>Store</code>: <code>2,020</code></li></ul><p>This is why the gap should not be described as &quot;just regalloc&quot;. The allocator amplifies an already oversized scalar boolean/bitfield program.</p><p>A phase-timed 45 second sample confirms the same split:</p><ul><li><code>eval_comb</code> merge input: <code>145,104</code> SIR instructions</li><li>ISel output: <code>495,111</code> MIR instructions</li><li>MIR after optimization: <code>263,530</code> instructions</li><li>MIR after regalloc and peepholes: <code>347,580</code> instructions</li><li>emitted <code>eval_comb</code> machine code: <code>1,764,047</code> bytes</li><li>runtime at timeout: <code>avg_comb_us=64.016</code>, <code>avg_apply_us=2.637</code></li></ul><p>The compile-time cost is visible too, especially <code>eval_comb</code> MIR optimization at about <code>4.63s</code>, but it is not the order-of-magnitude runtime gap. The runtime gap is already present in the per-tick hot code: <code>eval_comb</code> alone takes about ten times the Veryl cc per-cycle time.</p><p>A native priority-encoder lowering handles part of this shape by default. It reduces compile-only <code>eval_comb</code> instruction counts, but did not materially improve a same-conditions 60 second timed sample:</p><ul><li>without priority lowering: <code>avg_comb_us=61.508</code></li><li>with priority lowering: <code>avg_comb_us=62.280</code></li></ul><p>So the observed <code>Mux</code> volume is not only the already-recognized priority encoder shape, and the current lowering is not enough to close the runtime gap.</p><h2 id="hot-code" tabindex="-1">Hot Code <a class="header-anchor" href="#hot-code" aria-label="Permalink to &quot;Hot Code&quot;">​</a></h2><p>Function-level JIT perf map:</p><ul><li><code>[JIT]</code> code accounts for most runtime.</li><li><code>eval_comb</code> accounts for about <code>66%</code> of sampled cycles.</li></ul><p>Block-level JIT perf map:</p><ul><li><code>eval_comb.bb0</code>: about <code>23.6%</code></li><li><code>eval_comb.bb432</code>: about <code>20.2%</code></li><li><code>eval_comb.bb402</code>: about <code>5.1%</code></li><li><code>eval_comb.bb408</code>: about <code>3.2%</code></li></ul><p>The top blocks are very large:</p><ul><li><code>eval_comb.bb0</code>: about <code>597 KiB</code> of machine code</li><li><code>eval_comb.bb432</code>: about <code>501 KiB</code> of machine code</li></ul><p>MIR/regalloc stats for <code>eval_comb</code>:</p><ul><li>MIR after optimization: about <code>263k</code> instructions</li><li>MIR after regalloc: about <code>357k</code> instructions</li><li>regalloc adds about <code>93k</code> MIR instructions</li><li>stack reloads: about <code>41k</code></li><li>stack stores: about <code>21k</code></li><li>immediate reloads: about <code>28k</code></li></ul><p>Top block example:</p><ul><li><code>bb0</code> after MIR optimization: about <code>82.9k</code> instructions</li><li><code>bb0</code> after regalloc: about <code>113k</code> instructions</li><li><code>bb0</code> stack loads: about <code>13.9k</code></li><li><code>bb0</code> stack stores: about <code>7.8k</code></li></ul><p>Regalloc spill/reload attribution with <code>CELOX_REGALLOC_TRACE=1</code>:</p><ul><li><code>eval_comb</code> produced <code>91,848</code> regalloc spill/reload events.</li><li>Largest group: rematerialized immediate reloads, <code>28,340</code> events.</li><li>Largest stack reload groups: <ul><li><code>and</code> results: <code>8,484</code> reloads</li><li><code>cmp_imm</code> results: <code>6,173</code> reloads</li><li><code>sim_state_snapshot</code> loads: <code>4,347</code> reloads</li><li><code>sim_state_snapshot</code> <code>and_imm</code> results: <code>3,981</code> reloads</li></ul></li><li>Largest stack spill groups: <ul><li><code>and</code> results with next use beyond <code>1024</code> MIR positions: <code>1,974</code> spills</li><li><code>and</code> results with next use <code>257-1024</code>: <code>1,750</code> spills</li><li><code>cmp_imm</code> results with next use <code>257-1024</code>: <code>1,611</code> spills</li></ul></li></ul><p>This points to two separate problems: constants are still being loaded into registers too often after regalloc, and long-lived boolean/bitfield transient values are being kept across large regions and later reloaded from stack.</p><h2 id="what-the-hot-blocks-look-like" tabindex="-1">What The Hot Blocks Look Like <a class="header-anchor" href="#what-the-hot-blocks-look-like" aria-label="Permalink to &quot;What The Hot Blocks Look Like&quot;">​</a></h2><p>The beginning of <code>eval_comb.bb0</code> is mostly repeated scalar bitfield updates:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>load sim byte</span></span>
<span class="line"><span>and/or to clear or insert a bitfield</span></span>
<span class="line"><span>store sim byte</span></span>
<span class="line"><span>cmp</span></span>
<span class="line"><span>select const/0</span></span>
<span class="line"><span>or into a trigger or dirty byte</span></span>
<span class="line"><span>store sim byte</span></span></code></pre></div><p>The cost is not just &quot;full comb evaluation&quot;. Verilator-style full comb evaluation can be fast when generated code is compact and register allocation is good. The current Celox native path expands a large amount of scalar logic and then spills heavily.</p><p>After block-level MIR stats were expanded, <code>eval_comb</code> top blocks showed that the largest blocks are dominated by ALU and immediate ALU even before regalloc:</p><ul><li><code>eval_comb.bb0</code> after MIR optimization: <code>82,885</code> instructions, including <code>43,817</code> ALU, <code>16,760</code> ALU-immediate, <code>8,909</code> compares, and only <code>2,854</code> sim-state loads.</li><li><code>eval_comb.bb432</code> after MIR optimization: <code>74,696</code> instructions, including <code>39,088</code> ALU, <code>15,647</code> ALU-immediate, and <code>8,114</code> compares.</li></ul><p>So the primary issue is not memory access count alone. Many memory accesses are surrounded by scalar bit manipulation and trigger-byte accumulation logic.</p><h2 id="accepted-improvements" tabindex="-1">Accepted Improvements <a class="header-anchor" href="#accepted-improvements" aria-label="Permalink to &quot;Accepted Improvements&quot;">​</a></h2><h3 id="direct-full-variable-sub-byte-stores" tabindex="-1">Direct full-variable sub-byte stores <a class="header-anchor" href="#direct-full-variable-sub-byte-stores" aria-label="Permalink to &quot;Direct full-variable sub-byte stores&quot;">​</a></h3><p>Full-variable static stores where the declared variable width equals the store width no longer use bitfield read-modify-write lowering just because the width is not exactly 8/16/32/64 bits. For example, a whole 1-bit variable store now lowers to a masked byte store instead of:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>load dst byte</span></span>
<span class="line"><span>and clear unused destination bits</span></span>
<span class="line"><span>or source bit</span></span>
<span class="line"><span>store dst byte</span></span></code></pre></div><p>Measured Heliodor effect:</p><ul><li><code>eval_comb</code> after MIR optimization: about <code>263.5k</code> -&gt; <code>256.0k</code> instructions</li><li><code>eval_comb</code> after regalloc: about <code>347.6k</code> -&gt; <code>340.1k</code> instructions</li><li><code>eval_comb</code> sim-state loads: <code>6,158</code> -&gt; <code>3,625</code></li><li>40 second timeout sample still measured roughly <code>avg_comb_us=63.933</code>, so this reduces hot code size but is not sufficient by itself to close the runtime gap.</li></ul><p>The remaining top-of-block shape is now repeated direct full-variable stores followed by trigger-byte accumulation. That points at trigger/dirty byte accumulation and long boolean chains as the next places to attack, but those must be pressure-aware because earlier broad accumulation experiments worsened runtime.</p><h2 id="rejected-experiments" tabindex="-1">Rejected Experiments <a class="header-anchor" href="#rejected-experiments" aria-label="Permalink to &quot;Rejected Experiments&quot;">​</a></h2><p>The following changes were implemented locally and measured. They should not be reintroduced without a different safety or cost model.</p><h3 id="pre-regalloc-overwritten-sim-store-elimination" tabindex="-1">Pre-regalloc overwritten sim-store elimination <a class="header-anchor" href="#pre-regalloc-overwritten-sim-store-elimination" aria-label="Permalink to &quot;Pre-regalloc overwritten sim-store elimination&quot;">​</a></h3><p>Deleting same-block sim-state stores that are overwritten before a same-block load reduced MIR instruction count slightly, but worsened runtime.</p><p>Measured result:</p><ul><li><code>avg_comb_us</code> worsened from about <code>59.893</code> to about <code>65.051</code></li></ul><p>Reason:</p><ul><li>Many stores are useful lifetime cuts.</li><li>Removing them extends live ranges and makes regalloc/runtime worse even when MIR instruction count drops.</li></ul><h3 id="boolean-select-cond-const-0-lowering" tabindex="-1">Boolean <code>select(cond, const, 0)</code> lowering <a class="header-anchor" href="#boolean-select-cond-const-0-lowering" aria-label="Permalink to &quot;Boolean \`select(cond, const, 0)\` lowering&quot;">​</a></h3><p>Lowering boolean selects to <code>neg cond; and const</code> reduced some constant/select forms but worsened runtime.</p><p>Measured result:</p><ul><li><code>avg_comb_us</code> worsened to about <code>63.617</code></li></ul><p>Reason:</p><ul><li>The transform adds a transient value and can increase register pressure.</li><li>Local instruction-count reductions are not enough if live ranges get longer.</li></ul><h3 id="trigger-byte-pending-accumulation-in-isel" tabindex="-1">Trigger byte pending accumulation in ISel <a class="header-anchor" href="#trigger-byte-pending-accumulation-in-isel" aria-label="Permalink to &quot;Trigger byte pending accumulation in ISel&quot;">​</a></h3><p>Accumulating <code>triggered_bits</code> byte updates in a VReg until block exit did not materially reduce optimized MIR and worsened runtime.</p><p>Measured result:</p><ul><li><code>avg_comb_us</code> worsened to about <code>64.364</code></li></ul><p>Reason:</p><ul><li>Existing MIR store/load forwarding and DCE already remove much of the obvious redundancy.</li><li>Keeping the byte accumulator live increases pressure.</li></ul><h3 id="post-regalloc-same-block-dead-stack-store-elimination" tabindex="-1">Post-regalloc same-block dead stack-store elimination <a class="header-anchor" href="#post-regalloc-same-block-dead-stack-store-elimination" aria-label="Permalink to &quot;Post-regalloc same-block dead stack-store elimination&quot;">​</a></h3><p>Deleting stack spill stores that are not loaded again in the same block is incorrect.</p><p>Measured result:</p><ul><li>Heliodor runner segfaulted.</li></ul><p>Reason:</p><ul><li>Stack spill slots can be reloaded in successor blocks.</li><li>A same-block-only liveness test is not a valid proof that the spill store is dead.</li></ul><h3 id="regalloc-time-rematerialized-constant-folding" tabindex="-1">Regalloc-time rematerialized constant folding <a class="header-anchor" href="#regalloc-time-rematerialized-constant-folding" aria-label="Permalink to &quot;Regalloc-time rematerialized constant folding&quot;">​</a></h3><p>Folding rematerialized constants into MIR immediate operands during regalloc reduced the number of recorded regalloc events but worsened runtime.</p><p>Measured result:</p><ul><li><code>eval_comb</code> regalloc events fell from <code>91,848</code> to <code>75,998</code></li><li>rematerialized immediate reloads fell from <code>28,340</code> to <code>12,605</code></li><li><code>avg_comb_us</code> worsened from about <code>63.911</code> to about <code>64.565</code> in the trace-enabled 60 second sample</li></ul><p>Reason:</p><ul><li>Reducing allocator events is not the same as improving generated machine code.</li><li>This transform changes instruction selection after MIR optimization and can trade explicit reloads for less favorable immediate instruction forms without addressing the large scalar <code>And</code>/<code>Mux</code>/<code>LogicAnd</code>/<code>Eq</code> workload.</li><li>Immediate folding should be revisited in the x86 emitter or ISel cost model, not as a blind regalloc rewrite.</li></ul><h3 id="gvn-alias-algebraic-simplification" tabindex="-1">GVN alias algebraic simplification <a class="header-anchor" href="#gvn-alias-algebraic-simplification" aria-label="Permalink to &quot;GVN alias algebraic simplification&quot;">​</a></h3><p>GVN-time alias simplification for identities such as <code>x | 0</code>, <code>x ^ 0</code>, <code>x &amp; all_ones</code>, and <code>mux(c, x, x)</code> was also rejected.</p><p>Measured result:</p><ul><li>baseline <code>eval_comb</code> after regalloc: about <code>347k</code> MIR instructions</li><li>with the simplification: about <code>354k</code> MIR instructions</li></ul><p>Reason:</p><ul><li>Even width-safe aliasing can lengthen live ranges by deleting useful temporary definitions.</li><li>On this workload, the allocator pays more for the longer live ranges than the optimizer saves by removing local algebraic operations.</li></ul><h3 id="byte-aligned-dynamic-memory-access" tabindex="-1">Byte-aligned dynamic memory access <a class="header-anchor" href="#byte-aligned-dynamic-memory-access" aria-label="Permalink to &quot;Byte-aligned dynamic memory access&quot;">​</a></h3><p>Native ISel now tracks a conservative lower bound for the number of low zero bits in SIR integer registers. Dynamic bit offsets proven to be byte-aligned can use indexed byte addressing directly instead of always generating an intra-byte shift path.</p><p>Measured result:</p><ul><li><code>test_soc_linux_boot</code> 45 second Celox timeout sample: <code>avg_comb_us=59.49</code></li><li>Previous comparable samples were around <code>63us</code> per comb evaluation.</li><li>Hot indexed-memory FF blocks also dropped from about <code>683-685</code> MIR instructions after MIR opt to about <code>563-565</code>.</li></ul><p>Reason:</p><ul><li>Unpacked array element accesses usually have byte-aligned strides.</li><li>The SIR still represents offsets in bits, but native lowering no longer has to assume every dynamic offset may point into the middle of a byte.</li><li>This is a semantic-preserving unpacked-array improvement; it does not solve the remaining large scalar <code>And</code>/<code>Mux</code>/<code>Eq</code> workload by itself.</li></ul><h2 id="implications" tabindex="-1">Implications <a class="header-anchor" href="#implications" aria-label="Permalink to &quot;Implications&quot;">​</a></h2><p>The next optimization should be pressure-aware. Transformations that remove stores or introduce accumulator temporaries can be counterproductive because stores often shorten live ranges in the current allocator.</p><p>Promising directions:</p><ul><li>Add regalloc diagnostics that attribute each spill to the victim VReg, its defining instruction, next-use distance, and whether it is reloaded in the same block or a successor.</li><li>Fold rematerialized constants into immediate operands or rematerialize at the x86 operand level where possible, instead of materializing every use into a GPR before instruction selection.</li><li>Split long-lived boolean/bitfield transients when their next use is far away, especially <code>and</code> and <code>cmp_imm</code> results in large <code>eval_comb</code> blocks.</li><li>Improve allocator decisions before changing high-level store scheduling.</li><li>Prefer rematerialization and memory-operand folding that does not lengthen live ranges.</li><li>Consider block-local scheduling only with a register-pressure budget.</li><li>Treat broad store sinking and trigger-byte accumulation as unsafe until they are pressure-aware.</li></ul>`,100)])])}const b=o(l,[["render",c]]);export{m as __pageData,b as default};
