import{_ as a,o as t,c as i,ah as n}from"./chunks/framework.ojwJKwrc.js";const m=JSON.parse('{"title":"Optimization Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"internals/optimizations.md","filePath":"internals/optimizations.md"}'),s={name:"internals/optimizations.md"};function o(r,e,l,d,c,p){return t(),i("div",null,[...e[0]||(e[0]=[n(`<h1 id="optimization-architecture" tabindex="-1">Optimization Architecture <a class="header-anchor" href="#optimization-architecture" aria-label="Permalink to &quot;Optimization Architecture&quot;">​</a></h1><p>Celox optimizes at three compiler layers and once more in the runtime. Each layer uses the representation that can prove its transformations without depending on details from adjacent phases.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SLT structure</span></span>
<span class="line"><span>    ▼</span></span>
<span class="line"><span>SIR control flow and state accesses</span></span>
<span class="line"><span>    ▼</span></span>
<span class="line"><span>backend-private machine IR</span></span>
<span class="line"><span>    ▼</span></span>
<span class="line"><span>runtime event scheduling</span></span></code></pre></div><p>User-facing optimization presets and trade-offs are documented in <a href="/celox/guide/optimization-tuning.html">Optimization Tuning</a>. This page describes where the mechanisms live.</p><h2 id="symbolic-logic-layer" tabindex="-1">Symbolic logic layer <a class="header-anchor" href="#symbolic-logic-layer" aria-label="Permalink to &quot;Symbolic logic layer&quot;">​</a></h2><p>SLT optimizations operate while RTL expressions and bit ranges are still explicit.</p><ul><li><strong>Hash consing</strong> interns structurally identical expressions so common logic has one symbolic identity.</li><li><strong>Topological hoisting</strong> materializes shared subexpressions once at a legal dependency point.</li><li><strong>Range atomization</strong> splits state at observed bit boundaries, avoiding whole-value work when only a slice is required.</li><li><strong>Cost-directed mux lowering</strong> chooses between eager selection and control flow before expression structure is lost.</li></ul><p>This layer may use design dependency facts, but it does not know physical memory offsets or target instructions.</p><h2 id="sir-layer" tabindex="-1">SIR layer <a class="header-anchor" href="#sir-layer" aria-label="Permalink to &quot;SIR layer&quot;">​</a></h2><p><code>celox-sir-opt</code> owns backend-independent transformations over execution units and their control-flow graphs. The main families are:</p><table tabindex="0"><thead><tr><th>Family</th><th>Purpose</th></tr></thead><tbody><tr><td>Forwarding and coalescing</td><td>Reuse known state values and combine adjacent bit-range accesses</td></tr><tr><td>CFG simplification</td><td>Fold proven branches, remove unreachable blocks, and simplify block arguments</td></tr><tr><td>Value simplification</td><td>Eliminate duplicate expressions, redundant concatenations, and algebraic identities</td></tr><tr><td>Commit optimization</td><td>Avoid unnecessary Stable/Working copies and sink commits toward their producers</td></tr><tr><td>Dead state removal</td><td>Remove writes that are not observable under the selected preservation policy</td></tr><tr><td>Scheduling</td><td>Reorder independent work while preserving data and memory dependencies</td></tr><tr><td>Layout requirements</td><td>Record proven state aliases for validation during physical layout</td></tr></tbody></table><p>Passes exchange analysis results through SIR- and design-owned identities. They must not inspect Veryl syntax, assume x86 instruction costs as correctness facts, or mutate a finalized physical layout.</p><p>Some transformations are ordered deliberately. For example, control-flow and forwarding passes can expose dead values, while coalescing may create wider operations that a later target either keeps or splits. The pass manager owns this ordering; individual passes should not invoke one another as hidden pipelines.</p><h2 id="physical-layout-boundary" tabindex="-1">Physical layout boundary <a class="header-anchor" href="#physical-layout-boundary" aria-label="Permalink to &quot;Physical layout boundary&quot;">​</a></h2><p>Optimization may prove that two semantic state homes can share storage, but it does not assign byte offsets. It emits a layout requirement, and <code>celox-state-layout</code> validates width, state representation, and region compatibility before applying the alias.</p><p>This separation keeps semantic rewrites independent of packing decisions and prevents a backend from changing addresses after code generation begins.</p><h2 id="native-machine-layer" tabindex="-1">Native machine layer <a class="header-anchor" href="#native-machine-layer" aria-label="Permalink to &quot;Native machine layer&quot;">​</a></h2><p>The x86-64 backend lowers SIR into a private word-level SSA machine IR. At this point it can use target facts that would be inappropriate in SIR, including:</p><ul><li>immediate instruction forms and x86 operand constraints;</li><li>constant folding and algebraic simplification at machine width;</li><li>copy propagation, global value numbering, and dead-code elimination;</li><li>branch simplification and if-conversion;</li><li>known-bits reasoning and redundant-mask elimination;</li><li>target instruction selection for operations such as population count or bit extraction;</li><li>pressure-aware scheduling, spilling, and register allocation.</li></ul><p>Cranelift performs the corresponding target optimization through its own IR and pass pipeline. WebAssembly generation likewise remains backend-private. No machine-level result is written back into SIR.</p><h2 id="runtime-layer" tabindex="-1">Runtime layer <a class="header-anchor" href="#runtime-layer" aria-label="Permalink to &quot;Runtime layer&quot;">​</a></h2><p>Runtime optimizations avoid invoking compiled work when event semantics prove it unnecessary:</p><ul><li>an event that does not match the required edge does not evaluate its domain;</li><li>a non-cascaded single domain may use a combined evaluate-and-commit kernel;</li><li>cascaded evaluation continues only while newly triggered domains exist.</li></ul><p>These choices preserve the ordering model defined in <a href="./cascade-limitations.html">Runtime Semantics</a>. They do not change signal values or relax simulation consistency.</p><h2 id="correctness-boundary" tabindex="-1">Correctness boundary <a class="header-anchor" href="#correctness-boundary" aria-label="Permalink to &quot;Correctness boundary&quot;">​</a></h2><p>Every compiler transformation must preserve observable state, events, errors, and four-state value/mask behavior. A performance result can justify enabling or disabling a legal transformation, but it is never evidence that an otherwise unproven rewrite is correct.</p>`,26)])])}const u=a(s,[["render",o]]);export{m as __pageData,u as default};
