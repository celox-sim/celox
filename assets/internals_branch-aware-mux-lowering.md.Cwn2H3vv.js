import{_ as a,o as t,c as o,ah as n}from"./chunks/framework.ojwJKwrc.js";const u=JSON.parse('{"title":"Branch-aware mux lowering","description":"","frontmatter":{},"headers":[],"relativePath":"internals/branch-aware-mux-lowering.md","filePath":"internals/branch-aware-mux-lowering.md"}'),i={name:"internals/branch-aware-mux-lowering.md"};function r(s,e,l,c,d,h){return t(),o("div",null,[...e[0]||(e[0]=[n(`<h1 id="branch-aware-mux-lowering" tabindex="-1">Branch-aware mux lowering <a class="header-anchor" href="#branch-aware-mux-lowering" aria-label="Permalink to &quot;Branch-aware mux lowering&quot;">​</a></h1><p>Celox represents hardware selection as a <code>Mux</code>, but software simulation does not always benefit from evaluating both inputs and selecting afterward. The legacy compiler currently has two opportunities to preserve control flow. They share one profitability rule; they are not competing register allocation workarounds. This document describes that legacy boundary, not the final decision-region pipeline.</p><h2 id="relationship-between-the-two-stages" tabindex="-1">Relationship between the two stages <a class="header-anchor" href="#relationship-between-the-two-stages" aria-label="Permalink to &quot;Relationship between the two stages&quot;">​</a></h2><ol><li><strong>Cost-directed SLT lowering is the primary transform.</strong> While the source expression DAG is still available, the lowerer can put the exclusive part of each mux arm in separate SIR blocks. A common root or direct operand is materialized once in the dominator. If a non-trivial shared expression is deeper than that locally provable frontier, the conservative policy keeps the mux branchless instead of duplicating CSE work. This is where case/decoder control should normally be preserved, before eager lowering has lengthened all arm live ranges.</li><li><strong><code>BranchifyMux</code> is a post-lowering cleanup transform.</strong> It considers muxes that remain after lowering and SIR optimization. It may sink only pure, single-use arm definitions. Shared definitions remain in the head block. The transform uses the exact set of definitions that its legality repair will move when estimating cost, and charges values that must stay live across the new diamond.</li></ol><p>An SLT mux already lowered to CFG is no longer a SIR <code>Mux</code>, so the cleanup pass cannot branchify it a second time. The cleanup pass exists for opportunities that only become visible after SIR simplification, not as a replacement for source-DAG lowering.</p><p><code>BranchifyMux</code> is deliberately limited to locally proved, two-state regions. It does not claim that every Mux should become a branch: four-state Muxes, shared arm DAGs, impure operations, and transformations whose expected cost is not positive remain dataflow. A future whole-DAG decision-region optimization must use a verified rewrite plan rather than treating generic CFG/SSA verification as a semantic proof.</p><h2 id="local-expected-cost-decision" tabindex="-1">Local expected-cost decision <a class="header-anchor" href="#local-expected-cost-decision" aria-label="Permalink to &quot;Local expected-cost decision&quot;">​</a></h2><p>For true-arm work <code>T</code>, false-arm work <code>F</code>, and static true probability <code>p</code>, the work avoided by replacing a select with a branch is</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>saved = (1 - p) * T + p * F + select_cost</span></span></code></pre></div><p>The work introduced is</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>introduced = control</span></span>
<span class="line"><span>           + min(p, 1 - p) * mispredict_penalty</span></span>
<span class="line"><span>           + result_phi_copies</span></span>
<span class="line"><span>           + live_through_pressure</span></span></code></pre></div><p>All arithmetic is performed with integer probability weights. Conversion is legal only when <code>saved &gt; introduced</code>; equality stays as a mux. Instruction, phi, and live-through costs scale with the number of 64-bit chunks. Arithmetic such as multiplication and division is weighted by its lowered dynamic work, not treated as one SIR instruction.</p><p>Without profile data, an equality-to-constant decoder condition is predicted false (and inequality predicted true); other conditions use an even prior. <code>EqWildcard</code> and <code>NeWildcard</code> use the same rule in 2-state mode. This is a branch-probability heuristic, not an arm-count or function-size threshold. The prediction direction follows Ball and Larus, <a href="https://doi.org/10.1145/155090.155119" target="_blank" rel="noreferrer"><em>Branch Prediction for Free</em></a>, PLDI 1993. The 20/80 weight is a Celox engineering prior used in the absence of profile data; it is not a semantic rule or a theorem from that paper and must ultimately be calibrated against measured target behavior.</p><p>There is deliberately no global iteration, transformation-count, block-count, or code-size cap. A function may contain any number of profitable diamonds, but every diamond must independently pay for its branch, expected misprediction, merge copies, and downstream liveness cost.</p><h2 id="legality" tabindex="-1">Legality <a class="header-anchor" href="#legality" aria-label="Permalink to &quot;Legality&quot;">​</a></h2><ul><li>Four-state muxes remain dataflow muxes. A four-state <code>Mux</code> maps an X/Z condition to an all-X result, while the current SIR <code>Branch</code> tests only value bits; CFG conversion would therefore change behavior.</li><li>Only pure arm instructions may move. Stores, commits, runtime events, and capture events are never speculatively moved.</li><li><code>ForFold</code> is not a pure mux arm even when its capture-effect list is empty: it lowers to runtime loop control and includes non-progress <code>Error</code> exits.</li><li>A load may move only when no intervening write or event can conflict with it.</li><li>Definitions used by both arms or outside the candidate remain in their dominating block.</li><li>A mux result used after the split is represented by a merge parameter. A single immediate store may instead be distributed to the two arms when the mux result has no other use.</li></ul><h2 id="compile-time-complexity" tabindex="-1">Compile-time complexity <a class="header-anchor" href="#compile-time-complexity" aria-label="Permalink to &quot;Compile-time complexity&quot;">​</a></h2><p>SLT lowering walks the reachable expression DAG once to compute fanout. It memoizes owned dynamic cost, the width-independent region-slice lower bound, div/rem presence, purity, and the presence of non-trivial shared descendants per <code>NodeId</code>. Each mux then examines only its roots and direct operands. It does not rebuild a descendant set for every nested priority mux.</p><p>Arm lowering uses one cache with an insertion log. On leaving an arm it rolls back only entries inserted in that arm; it never clones the global materialized node cache. Analysis is therefore linear in reachable DAG nodes and edges, and cache maintenance is linear in newly lowered arm nodes, independent of a large unrelated global cache. Depth-chain and unrelated-cache regression tests enforce those bounds; there is no depth or node-count cutoff.</p><h2 id="acceptance-gates" tabindex="-1">Acceptance gates <a class="header-anchor" href="#acceptance-gates" aria-label="Permalink to &quot;Acceptance gates&quot;">​</a></h2><p>Correctness is checked by SIR verification before and after optimization and by focused tests for shared definitions, aliasing loads, merge parameters, dominating live-ins, four-state mode, and rejected break-even candidates. Local same-build <code>avg_comb_us</code> A/B measurements are diagnostics only. A compile-only reduction, a smaller SIR/MIR count, or a partial runtime window is not an acceptance result. The sole performance gate is the pinned same-input full Heliodor Linux-boot run implemented by <code>scripts/run-heliodor-bench.sh gate</code>: both <code>veryl-cc</code> and Celox must report <code>status=pass</code>, Celox must report <code>compile_only=false</code>, and Celox wall time must not exceed the corresponding Veryl wall time.</p><h2 id="current-boundary-and-next-region-transform" tabindex="-1">Current boundary and next region transform <a class="header-anchor" href="#current-boundary-and-next-region-transform" aria-label="Permalink to &quot;Current boundary and next region transform&quot;">​</a></h2><p>Binary reverse if-conversion is only the leaf mechanism. On the pinned Heliodor input it converts 2,579 of 22,344 muxes and improves the partial-run <code>avg_comb_us</code> from roughly 48 us to 36.3 us, but it does not make the full run competitive. Another 3,227 muxes already pass the expected-runtime cost test and are rejected solely because a nontrivial DAG node is shared below the arm roots. Treating those as independent diamonds would either duplicate code or lose CSE.</p><p>The production replacement is whole-unit, occurrence- and token-aware DAG placement. Source provenance and the occurrence action skeleton are verified first; token SSA then creates versioned <code>InstValue</code>s, execution-safety analysis limits their legal domains, and ScheduleEarly/ScheduleLate derives state-specific use envelopes. Bottom-up gate/decision selection is followed by one final <code>PlacementPlan</code>, which assigns each pure <code>InstValue</code> once to its latest legal dominating control site. This is the gated-SSA/lazy-code-motion relationship: profitability selects control regions while verified placement owns shared pure work. Raw <code>NodeId</code> reachability or nearest-common-dominator placement is not the final value identity. The design uses no mux, block, iteration, or traversal cap.</p><p>Long mutually exclusive equality chains form a higher-level <code>DecisionRegion</code>. After proving selector identity, key width, uniqueness/mask overlap, priority, default behavior, arm purity, and successor argument types, target lowering may select a value lookup table, jump table, balanced comparison tree, ordered early-exit chain, or branchless tail. This does not compete with binary mux lowering: a decision region owns the tests, dominance-aware placement owns its shared DAG, and ordinary mux lowering handles the residual leaves.</p>`,25)])])}const m=a(i,[["render",r]]);export{u as __pageData,m as default};
