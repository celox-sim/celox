import{_ as e,o as i,c as a,ah as t}from"./chunks/framework.ojwJKwrc.js";const k=JSON.parse('{"title":"Development environment","description":"","frontmatter":{},"headers":[],"relativePath":"development.md","filePath":"development.md"}'),n={name:"development.md"};function o(l,s,h,p,d,r){return i(),a("div",null,[...s[0]||(s[0]=[t(`<h1 id="development-environment" tabindex="-1">Development environment <a class="header-anchor" href="#development-environment" aria-label="Permalink to &quot;Development environment&quot;">​</a></h1><p>The Linux development environment is defined in <code>flake.nix</code> and <code>flake.lock</code>. It supports x86-64 and AArch64. The host and both devcontainer variants use the same tools: Rust, Node.js 24, pnpm, Python with cocotb, Verilator, C/C++ build tools, mold, cargo-insta, and mbx. Rust reads <code>rust-toolchain.toml</code>. Corepack selects pnpm from the nearest <code>package.json</code> with a <code>packageManager</code> field, so pnpm updates do not depend on the version available in nixpkgs. The first use of a version requires network access to download it into the user&#39;s Corepack cache; subsequent uses can run offline. No <code>corepack enable</code> step is needed.</p><h2 id="enter-the-environment" tabindex="-1">Enter the environment <a class="header-anchor" href="#enter-the-environment" aria-label="Permalink to &quot;Enter the environment&quot;">​</a></h2><p>Install Nix with <code>nix-command</code> and <code>flakes</code> enabled, then run:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nix</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> develop</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --frozen-lockfile</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cargo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --locked</span></span></code></pre></div><p>For scripts, use <code>nix develop --command &lt;command&gt;</code>, for example:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nix</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> develop</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --command</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build:napi</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nix</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> develop</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --command</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cargo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --locked</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> celox-vpi</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --test</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cocotb_e2e</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  cocotb_drives_an_attached_native_flip_flop</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --exact</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --ignored</span></span></code></pre></div><p>The shell sets <code>CELOX_COCOTB_PYTHON</code> to its Python with cocotb. It includes <code>wasm32-unknown-unknown</code> for browser Rust builds. The separate NAPI WASI cross-build still needs the WASI target and SDK used by CI; these are not part of this native development shell.</p><p>Native builds in the shell use mold through the Nix compiler wrapper to reduce link time. This applies to both supported Linux architectures and the devcontainer shells. No global <code>RUSTFLAGS</code> or repository-wide Cargo linker setting is needed, so WebAssembly builds and builds outside this environment keep their own linker configuration. Re-enter the shell after updating the flake to activate mold.</p><p>For automatic activation, install direnv and nix-direnv once:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nix</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> profile</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nixpkgs#direnv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nixpkgs#nix-direnv</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mkdir</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/.config/direnv</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">printf</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;\\n%s\\n&#39;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;source &quot;$HOME/.nix-profile/share/nix-direnv/direnvrc&quot;&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/.config/direnv/direnvrc</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">printf</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;\\n%s\\n&#39;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;eval &quot;$(direnv hook bash)&quot;&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/.bashrc</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">eval</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">direnv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> hook bash)&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">direnv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> allow</span></span></code></pre></div><p>For zsh, replace the two Bash hook commands with:</p><div class="language-zsh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">zsh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">printf</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;\\n%s\\n&#39;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;eval &quot;$(direnv hook zsh)&quot;&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/.zshrc</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">eval</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">direnv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> hook zsh)&quot;</span></span></code></pre></div><p>The checked-in <code>.envrc</code> loads the flake through nix-direnv. Review and allow it again when it changes. Launch editors from the activated shell, or use an editor&#39;s direnv integration, so subprocesses get the same tools.</p><h2 id="global-npm-tools" tabindex="-1">Global npm tools <a class="header-anchor" href="#global-npm-tools" aria-label="Permalink to &quot;Global npm tools&quot;">​</a></h2><p>The devShell directs global npm installs (including CLI self-updates) to <code>~/.local/share/npm</code> and adds its <code>bin</code> directory to PATH. Set <code>NPM_CONFIG_PREFIX</code> before entering the shell to use another writable location. The devcontainer configures the same location for its user. Do not use sudo or try to change permissions in <code>/nix/store</code>.</p><p>After changing the flake, re-enter <code>nix develop</code> or run <code>direnv reload</code>. An already-running shell can apply the same settings immediately:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> NPM_CONFIG_PREFIX</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$HOME</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/.local/share/npm&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> PATH</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$NPM_CONFIG_PREFIX</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/bin:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$PATH</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -g</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @openai/codex</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">hash</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -r</span></span></code></pre></div><h2 id="mbx-and-filesystem-placement" tabindex="-1">mbx and filesystem placement <a class="header-anchor" href="#mbx-and-filesystem-placement" aria-label="Permalink to &quot;mbx and filesystem placement&quot;">​</a></h2><p>Inside the development environment, <code>cargo</code> is an mbx shim. This also applies to Cargo invoked by pnpm or rust-analyzer. The real Cargo and Rust toolchain remain on PATH; no global <code>mbx setup</code> or Cargo configuration is modified. <code>mbx doctor</code> may still warn that global setup is missing: the shell uses a scoped Cargo symlink. Check <code>readlink -f &quot;$(command -v cargo)&quot;</code> inside the shell; it should resolve to the Nix-provided mbx binary.</p><p>mbx keeps reading your per-user configuration. On the existing development host, <code>~/mbx</code> is a mounted Btrfs filesystem and <code>~/.config/mbx/config.toml</code> already selects its <code>cache</code> directory. The flake does not override it. Machine-specific absolute paths do not belong in <code>flake.nix</code> or <code>.mbx.toml</code>.</p><p>On another Linux host, create a writable directory on your chosen local filesystem and add this to <code>~/.config/mbx/config.toml</code> (use your actual absolute path; TOML does not expand <code>$HOME</code>):</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cache_dir = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/home/YOUR_USER/mbx/cache&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Optional limits; choose these for your disk and workload.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">gc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">max_total_size = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;50GiB&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">target</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">max_age = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;30d&quot;</span></span></code></pre></div><p>Alternatively, export <code>MBX_CACHE_DIR=/your/mounted/filesystem/cache</code> before entering the shell. This takes precedence over the user configuration. Managed targets default to a directory inside the cache, so cache blobs and restored outputs stay on the same filesystem. Avoid setting <code>CARGO_TARGET_DIR</code>, Cargo&#39;s <code>build.target-dir</code>, or a separate <code>MBX_TARGET_ROOT</code>: explicit Cargo targets bypass managed placement, and another filesystem prevents reflinks.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mbx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> doctor</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cargo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> check</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --locked</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> celox-state-layout</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">readlink</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> target</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mbx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --dry-run</span></span></code></pre></div><p>For a new checkout, the first build creates <code>target</code> as a symlink into the managed cache. If a real <code>target/</code> already exists, mbx asks interactively before replacing its outputs; non-interactive builds leave it alone. Stop active builds before migrating. To discard old build outputs explicitly, use <code>cargo clean</code> and remove an empty <code>target/</code> if necessary, then build again. Do not delete the shared cache to migrate one checkout.</p><p>Btrfs is optional: mbx copies when reflinks are unavailable, so caching and collection still work, but shared outputs can take more physical space. Verify both cache and managed-target reflinks with <code>mbx doctor</code>. GC budgets use logical bytes and are not hard limits on physical disk consumption.</p><h3 id="provisioning-a-btrfs-cache" tabindex="-1">Provisioning a Btrfs cache <a class="header-anchor" href="#provisioning-a-btrfs-cache" aria-label="Permalink to &quot;Provisioning a Btrfs cache&quot;">​</a></h3><p>If an existing writable Btrfs mount is available, use a directory on it. Otherwise, an administrator can provision a dedicated filesystem or a loopback image once, mount it persistently through <code>/etc/fstab</code>, and give your user ownership of a cache directory. Normal builds and reflinks need no sudo. Neither the flake nor devcontainer mounts a block filesystem.</p><p>For the existing loopback setup, check the mount before building:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">findmnt</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --target</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$HOME</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/mbx&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">df</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -h</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$HOME</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/mbx&quot;</span></span></code></pre></div><p>The filesystem type should be <code>btrfs</code>, not the parent filesystem. A loopback image consumes space on its backing filesystem too; monitor both. Nix&#39;s <code>/nix/store</code> and its garbage collection are separate from mbx&#39;s storage budget.</p><h2 id="devcontainer" tabindex="-1">Devcontainer <a class="header-anchor" href="#devcontainer" aria-label="Permalink to &quot;Devcontainer&quot;">​</a></h2><p>Both <code>.devcontainer/default</code> and <code>.devcontainer/with-claude</code> directly use the Ubuntu 24.04 devcontainer base image, install Nix, and build <code>.#dev-tools</code> from the same lockfile. The resulting profile is on the editor&#39;s PATH, and Bash loads the full devShell environment. Tool installation failures stop setup. Optional assistant installations retain their existing opt-in behavior and are outside the pinned development toolchain.</p><p>Before opening the container, ensure <strong><code>$HOME/mbx</code> exists on the Docker host</strong> and, if using Btrfs, that it is mounted. Both variants bind that directory to <code>/home/vscode/mbx</code> and set <code>MBX_CACHE_DIR=/home/vscode/mbx/container-cache</code>. The container cache and managed targets are on the same host filesystem, without granting mount privileges to the container. Docker Desktop or remote filesystem sharing may not preserve reflink support; <code>mbx doctor</code> inside the container is the definitive check. The host directory must be writable by the container user (normally the devcontainer aligns its UID with the local user).</p><p>For a different host path, edit the mbx <code>mounts</code> entry in your chosen <code>devcontainer.json</code>. You can use <code>source=\${localEnv:CELOX_MBX_ROOT}</code> and set <code>CELOX_MBX_ROOT</code> in the environment that launches VS Code / the devcontainer CLI. Keep the destination and <code>MBX_CACHE_DIR</code> unchanged. With a remote Docker daemon, the source must exist on that daemon&#39;s host.</p><p>Host and container paths differ. A <code>target</code> symlink created on the host may point outside the container, and vice versa. Prefer separate checkouts for host and container builds. Keep their cache roots separate as configured: mbx collects targets whose checkout paths no longer exist, and each environment sees different absolute paths. Worktrees within one environment still share its cache. Both cache roots use the same Btrfs filesystem. If reusing one checkout, stop builds and remove only the <code>target</code> symlink (<code>unlink target</code>) before switching environments, then let mbx recreate it. Never use recursive deletion on the symlink&#39;s resolved cache directory.</p><p>After creating the container, run <code>mbx doctor</code> and a build in its terminal. Re-run <code>bash .devcontainer/post-create.sh</code> after changing the flake/lockfile, then restart terminals and reload the editor to refresh its tool profile.</p><h2 id="updating-and-validating-the-environment" tabindex="-1">Updating and validating the environment <a class="header-anchor" href="#updating-and-validating-the-environment" aria-label="Permalink to &quot;Updating and validating the environment&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nix</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> flake</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> update</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nix</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> flake</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> check</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nix</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> fmt</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --check</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> flake.nix</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nix</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> develop</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --command</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bash</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;rustc --version; node --version; pnpm --version; mbx doctor&#39;</span></span></code></pre></div><p>Commit <code>flake.lock</code> together with environment changes. When updating Rust, update the rust-overlay input if its locked revision predates the release. When updating pnpm, update <code>packageManager</code> in <code>package.json</code> and the pnpm lockfile as needed; no Nix version or hash update is required. Verify with <code>nix develop --command pnpm --version</code> (also from a workspace package directory). mbx comes from <a href="https://github.com/tignear/nix-packages" target="_blank" rel="noreferrer">tignear/nix-packages</a>. Run <code>nix flake update nix-packages</code> to update its pinned package definition.</p><p>The shell pins development tools, not the complete host OS or all build inputs. Use the committed Cargo/pnpm lockfiles and locked dependency installation for repeatable builds. Existing CI workflows continue to test their platform matrix independently of the Linux devShell.</p>`,42)])])}const g=e(n,[["render",o]]);export{k as __pageData,g as default};
