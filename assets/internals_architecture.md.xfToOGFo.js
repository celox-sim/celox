import{_ as a,o as n,c as t,ah as s}from"./chunks/framework.ojwJKwrc.js";const u=JSON.parse('{"title":"Simulator Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"internals/architecture.md","filePath":"internals/architecture.md"}'),i={name:"internals/architecture.md"};function o(r,e,l,c,d,p){return n(),t("div",null,[...e[0]||(e[0]=[s(`<h1 id="simulator-architecture" tabindex="-1">Simulator Architecture <a class="header-anchor" href="#simulator-architecture" aria-label="Permalink to &quot;Simulator Architecture&quot;">​</a></h1><p>Celox compiles Veryl RTL into executable simulation kernels and runs them with an event-driven runtime. The architecture is optimized for synchronous RTL verification rather than gate-level timing or detailed delta-cycle emulation.</p><h2 id="system-overview" tabindex="-1">System overview <a class="header-anchor" href="#system-overview" aria-label="Permalink to &quot;System overview&quot;">​</a></h2><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Veryl source</span></span>
<span class="line"><span>    │</span></span>
<span class="line"><span>    ▼</span></span>
<span class="line"><span>frontend analysis and hierarchy elaboration</span></span>
<span class="line"><span>    │</span></span>
<span class="line"><span>    ▼</span></span>
<span class="line"><span>symbolic logic (SLT) and dependency scheduling</span></span>
<span class="line"><span>    │</span></span>
<span class="line"><span>    ▼</span></span>
<span class="line"><span>Simulator IR (SIR) and backend-independent optimization</span></span>
<span class="line"><span>    │</span></span>
<span class="line"><span>    ▼</span></span>
<span class="line"><span>physical state layout</span></span>
<span class="line"><span>    │</span></span>
<span class="line"><span>    ├──► native x86-64 code</span></span>
<span class="line"><span>    ├──► Cranelift JIT code</span></span>
<span class="line"><span>    └──► WebAssembly</span></span>
<span class="line"><span>             │</span></span>
<span class="line"><span>             ▼</span></span>
<span class="line"><span>      event-driven runtime</span></span></code></pre></div><p>Compilation uses distinct artifacts for each phase. Source-language objects do not leak into optimization or code generation, and target-specific IR does not leak back into SIR. See <a href="./compiler-crate-architecture.html">Compiler Components</a> for the crate and artifact boundaries.</p><h2 id="frontend-and-scheduling" tabindex="-1">Frontend and scheduling <a class="header-anchor" href="#frontend-and-scheduling" aria-label="Permalink to &quot;Frontend and scheduling&quot;">​</a></h2><p>The Veryl frontend analyzes modules, elaborates hierarchy, and records the source lookup information needed by diagnostics and public signal paths. Combinational expressions are represented as symbolic logic trees (SLT).</p><p>The scheduler then:</p><ol><li>flattens the elaborated hierarchy;</li><li>assigns source-independent state identities;</li><li>derives combinational dependencies and clock domains;</li><li>orders combinational work and detects dependency cycles;</li><li>lowers scheduled logic into SIR execution units.</li></ol><p>After this transition, downstream phases use design identities and SIR rather than Veryl parser nodes or SLT arena identifiers. The scheduling algorithm is described in <a href="./combinational-analysis.html">Combinational Analysis</a>.</p><h2 id="sir-optimization-and-layout" tabindex="-1">SIR, optimization, and layout <a class="header-anchor" href="#sir-optimization-and-layout" aria-label="Permalink to &quot;SIR, optimization, and layout&quot;">​</a></h2><p>SIR is the common source- and target-independent control-flow IR. It represents bit-precise loads, stores, arithmetic, selection, and the execution units needed for combinational and sequential phases.</p><p>Backend-independent passes simplify and reschedule SIR before physical addresses are assigned. Layout then maps semantic state objects into regions of one simulation buffer:</p><ul><li><strong>Stable</strong> contains committed signal and register values.</li><li><strong>Working</strong> contains next-state values during split-phase sequential evaluation.</li><li><strong>Sparse working metadata</strong> tracks state that only needs selective commit.</li><li><strong>Triggered bits</strong> record event-producing changes.</li><li><strong>Scratch</strong> is backend-requested temporary storage when a kernel must be split.</li></ul><p>Four-state objects store a value plane and a mask plane. Layout is immutable once code generation begins, so every backend sees the same state representation.</p><p>See <a href="./ir-reference.html">SIR Reference</a> and <a href="./optimizations.html">Optimization Architecture</a> for these layers.</p><h2 id="backends" tabindex="-1">Backends <a class="header-anchor" href="#backends" aria-label="Permalink to &quot;Backends&quot;">​</a></h2><p>All backends consume laid-out SIR and implement the runtime&#39;s backend contract.</p><h3 id="native-x86-64" tabindex="-1">Native x86-64 <a class="header-anchor" href="#native-x86-64" aria-label="Permalink to &quot;Native x86-64&quot;">​</a></h3><p>The default x86-64 backend lowers SIR to a private machine IR, performs machine-level optimization and register allocation, and emits executable code. Its instruction selection, allocation data, and executable-memory management are not part of the shared compiler model.</p><h3 id="cranelift" tabindex="-1">Cranelift <a class="header-anchor" href="#cranelift" aria-label="Permalink to &quot;Cranelift&quot;">​</a></h3><p>The Cranelift backend translates SIR to Cranelift IR and uses Cranelift&#39;s JIT. It is the native fallback where the custom x86-64 backend is unavailable.</p><h3 id="webassembly" tabindex="-1">WebAssembly <a class="header-anchor" href="#webassembly" aria-label="Permalink to &quot;WebAssembly&quot;">​</a></h3><p>The WebAssembly backend translates the same SIR and layout into a Wasm module. It supports both the Rust host path and the browser playground.</p><h2 id="runtime-execution" tabindex="-1">Runtime execution <a class="header-anchor" href="#runtime-execution" aria-label="Permalink to &quot;Runtime execution&quot;">​</a></h2><p>The runtime owns scheduling and observable simulator behavior. A simulation step has four conceptual stages:</p><ol><li>take all events scheduled for the current time;</li><li>detect clock edges and collect triggered domains;</li><li>evaluate next state from the current stable state;</li><li>commit updates and repeat combinational propagation until the step settles.</li></ol><p>When several domains trigger together, evaluation and commit are separated so that every domain reads the same pre-update state. If a committed result drives another clock, the runtime discovers that domain and continues within the same step. <a href="./cascade-limitations.html">Runtime Semantics</a> describes this behavior and its boundaries.</p><p>A running simulator retains the elaborated design, source lookup, runtime schema, bound testbench bytecode, and compiled backend. The backend owns the finalized layout. Frontend and optimizer state are discarded after compilation.</p><h2 id="public-api-boundary" tabindex="-1">Public API boundary <a class="header-anchor" href="#public-api-boundary" aria-label="Permalink to &quot;Public API boundary&quot;">​</a></h2><p>The Rust <code>celox</code> crate is a facade over the compiler and runtime components. The Node binding and TypeScript package expose that facade to user testbenches. API options select compiler policy or runtime behavior, but callers do not construct phase artifacts or depend on concrete backend internals.</p>`,31)])])}const m=a(i,[["render",o]]);export{u as __pageData,m as default};
