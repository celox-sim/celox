{
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "pnpm": {
    "overrides": {
      "@celox-sim/celox": "workspace:*",
      "@celox-sim/vite-plugin": "workspace:*"
    },
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
