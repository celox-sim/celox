module Top (
    i_clk: input clock,
    i_rst: input reset,
    d:     input logic<8>,
    q:     output logic<8>,
) {
    always_ff (i_clk, i_rst) {
        if_reset {
            q = 0;
        } else {
            q = d;
        }
    }
}
